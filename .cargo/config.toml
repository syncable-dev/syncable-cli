[build]
rustflags = ["-C", "target-cpu=native"]

[alias]
b = "build"
br = "build --release"
c = "check"
t = "test"
tr = "test --release"
r = "run"
rr = "run --release"
# Note: cargo fmt and clippy aliases removed - were recursive and broke CI